# The web application's context alone; the `api` service builds under api/.dockerignore. The first
# block is IGNORE in .dagger/src/index.ts, so the two lists stay in step.
**/build
**/.gradle
**/.kotlin
.dagger/sdk
**/node_modules
**/dist
**/coverage
**/src/paraglide

api/
docs/
agents/
security/
.git
