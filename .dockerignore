# The web application's context alone; the `api` service builds under api/.dockerignore. The first
# block was copied from IGNORE in .dagger/src/index.ts on 2026-09-18, and nothing keeps the two in step.
**/build
**/.gradle
**/.kotlin
.dagger/sdk
**/node_modules
**/dist
**/coverage
**/src/paraglide

api/
docs/
agents/
security/
.git
