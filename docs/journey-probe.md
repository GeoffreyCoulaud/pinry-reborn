# Journey probe

A throwaway file. It exists to make one pull request whose every changed path ends in `.md`, so that
`validate.yml`'s documentation-only shortcut can be observed. Delete the branch and the pull request
once the run has concluded.
