# Journey probe

A throwaway file. It exists to make one pull request that changes a `.md` path and a `.kt` path, so
that `validate.yml` running the full gate on a mixed pull request can be observed. Delete the branch
and the pull request once the run has concluded.
