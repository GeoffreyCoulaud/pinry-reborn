Facts about this block that the diff does not show. Plain notes, in no particular order.

Position: block 10 of the lot `the-data-travels`, the bottom of a stack of pull requests; base `main`. The next pull request up the stack is block 20. This block also carries the lot's specification.
Gate: `dagger call gate` green at 699d7959, 2 min 48 s, exit 0. The last commit, eef0c88f, only puts a comment back to its text on main.
Continuous integration: run 36143519541, green, verify 7 min 40 s.
Budget against main: 163 lines, 19 files.
contract-guard: green at 18.0.0; red at 17.1.0 with `api-major-version-not-bumped` (checked by setting 17.1.0, regenerating, then restoring).
Mutations tried: with the 17.0.0 contract restored, the states test fails (`expected: <[PENDING, READY, FAILED, EXPIRED, DELETED, SUPERSEDED]> but was: <[]>`); with an `enum` added to `kind`, the open-kind test fails.
Tier-1 fix: the ImportProducers KDoc said "The three import beans"; now "The import beans".
Tier-2 questions: none.
Departure: one client file changed, clients/apps/webapp/src/lib/uploads.test.ts: its LIMITS fixture is typed by the generated client and the two new LimitsDto fields are required, so the clients' typecheck refused it.
Departure: ImportUploadBounds is a data class, not a value class: it holds two fields.
Departure: a tier-1 fix left out for the file bound: the comment on ImportsConfig.maxChunkBytes names the wrong test (ImportsConfigIntegrationTest instead of BodyLimitCheck); fixing it made 20 files, so it is left to block 30.
Pitfall: a required field added to a DTO breaks the clients' typed fixtures, not the Gradle build; only the full gate caught it.
Pitfall: `gh stack submit --auto` titles the pull request after the branch and writes no body.
Pitfall: the pre-push gate did not show in `gh stack submit`'s output; whether it runs through gh stack is not established.
Consumers: the states are first read by block 20 (export section), the two bounds by block 30 (upload).
