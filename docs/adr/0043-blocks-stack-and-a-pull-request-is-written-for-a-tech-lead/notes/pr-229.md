Facts about this block that the diff does not show. Plain notes, in no particular order.

Position: the closing block of the lot `the-data-travels`, stacked on #228 (block 65) at the top of a stack of ten pull requests, none merged yet. It fixes the holistic review's findings: 1 MAJOR and 11 MINOR, in .reviews/the-data-travels-holistic.md (not committed). The review ran on the top of the stack before any merge, at the operator's request. The block also corrects the handoff, reconciles the backlog and corrects three contradictions in the specification.
The findings, as the review stated them:
- MAJOR: once an upload stopped, the store kept it and it outranked the server's row in the section and the task centre, with Cancel as its only control; a close request whose answer was lost turned into a stop, while the import could already be PENDING or RUNNING on the server, and Cancel would DELETE it. The losing tab of the two-tabs case was in the same position.
- MINOR: a successful upload briefly showed "The upload of pinry.zip stopped before the end. Choose that file again" for one round trip.
- MINOR: a cancel refused from the upload view was silent, the component holding the mutation having unmounted.
- MINOR: a read of the latest import already under way could delete the resume record of the import just opened (window refocus when the file dialog closes).
- MINOR: an API comment change (ImportsConfig) arrived in a web application block.
- MINOR: the journeys' row fixtures accepted any string as a state.
- MINOR: redundant empty list routes left in three journeys after block 60 added default handlers.
- MINOR: no test for a refused opening of an import.
- MINOR: formatting churn in the message catalogues.
- MINOR: comments cited "decision D1" or "section 2" without naming the document.
- MINOR: the specification contradicted its own corrections (header, section 5, section 6).
- MINOR: maxImportArchiveBytes had no KDoc.
Exits: all fixed, except the ImportsConfig one, kept and recorded in the handoff. Two findings were partly wrong: #222's 264 lines do count the ImportsConfig comment, and maxFileBytes and maxPixels carry no KDoc either.
Operator decision behind the two-tabs text: "Q1", the two-tabs resume risk is an accepted known limit with no guard.
Gate: `dagger call gate` green at 6de14f7b, exit 0; clients 55 files, 280 tests; src/lib coverage 100 % of lines and branches.
Tests red before the fix: the three new behaviour cases (the flash, the lost close, the refused cancel) failed against the unfixed store. The refused-opening case passed already: a coverage gap, not a defect.
Budget against feat/the-task-centre-keeps-data-notices: 278 lines, 18 files.
Continuous integration: run 36160705746, green, verify 7 min 49 s.
Headless reading (Firefox, stubbed API, 1280 and 380 px, the section's text sampled every 25 ms): the success goes from 100 % straight to "The archive is waiting to be imported." with no frame of the reload sentence; with every close cut for a second, the section holds at 100 % and sends the close again 5 s later, then the 409 hands over to the pending import; a refused cancel shows "That could not be done. Try again in a moment." under the running progress bar. No horizontal overflow at 380 px.
Pitfall: Firefox resends a POST cut on a reused connection by itself, up to four times within the same millisecond; a stub that cuts only the first close tests the browser's retry, not the application's.
Documents: the handoff's merge figures and the operator's reading stay to fill after the review.
Tier-2 questions: none in this block.
