Facts about this block that the diff does not show. Plain notes, in no particular order.

Position: block 37 of the lot `the-data-travels`, the last of three blocks block 30 was split into (30, 34, 37) after block 30 measured 892 lines; operator's answer "b", 2026-09-25. Stacked on #223 (block 34, branch feat/the-import-is-followed), which is stacked on #222 (block 30: the upload store and its pure decisions). The next block up is 40 (resuming after a reload).
Gate: `dagger call gate` green at 08412ddd, exit 0.
Continuous integration: run 36149285625, green, verify 6 min 41 s.
Budget against feat/the-import-is-followed: 367 lines, 7 files.
Headless reading: Firefox over WebDriver BiDi, bundle served against a stubbed API that cuts sockets or answers 507 on demand, at 1280 and 380 px: uploading at 52 %, the cancel confirmation during an upload, paused after the five attempts (23 s real), stopped by a 507, a 60 MB file against a 50 MB bound. No horizontal overflow; nothing needed fixing.
The three blocks together differ from the single 892-line block only by #223's two display fixes and this journey's openTheAccount helper taking its answer as an option.
Tier-1 fixes: none.
Tier-2 questions: the split above, carried by #222.
Pitfall: jsdom's Blob does not survive Node's fetch: a slice of a jsdom File reaches MSW as the text "undefined", so the journey builds its archive with node:buffer's File. The browser is unaffected.
Pitfall: the retry wait is real time, so the journeys use fake timers with shouldAdvanceTime and advance by RETRY_MS.
Pitfall: a stopped upload leaves the import AWAITING_ARCHIVE on the server; the section then offers cancel and no picker. Block 40 adds resuming with the same file.
Pitfall: whether one PUT at a time with a 5-second wait suffices against a half-open connection is not measured (specification section 9).
Departure: this block is the rest of block 30's row after 34's cancel case.
