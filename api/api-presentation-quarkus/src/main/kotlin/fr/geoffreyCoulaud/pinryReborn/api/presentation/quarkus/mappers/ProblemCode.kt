package fr.geoffreyCoulaud.pinryReborn.api.presentation.quarkus.mappers

/**
 * Every `code` a problem can carry on the wire: those the framework or the HTTP layer decide, then one per
 * `ErrorCode`, which [BaseErrorMapper] maps (`docs/adr/0042-the-presentation-owns-the-refusal-codes.md`).
 */
enum class ProblemCode {
    VALIDATION_ERROR,
    MALFORMED_BODY,
    UNKNOWN_ROUTE,
    METHOD_NOT_ALLOWED,
    UNSUPPORTED_MEDIA_TYPE,
    NOT_ACCEPTABLE,
    HTTP_ERROR,
    INTERNAL_ERROR,
    AUTHENTICATION_REQUIRED,
    AUTHENTICATION_FAILED,
    SESSION_EXPIRED,
    RANGE_NOT_SATISFIABLE,
    BODY_TOO_LARGE,
    USERNAME_ALREADY_EXISTS,
    USER_DOES_NOT_EXIST,
    INVALID_PASSWORD,
    INVALID_HTTP_AUTHORIZATION_SCHEME,
    PIN_DOES_NOT_EXIST,
    PIN_INSUFFICIENT_PERMISSIONS,
    PIN_NOT_SOFT_DELETED,
    PIN_ALREADY_SOFT_DELETED,
    SEARCH_EMPTY_QUERY,
    IMAGE_DOES_NOT_EXIST,
    IMAGE_INSUFFICIENT_PERMISSIONS,
    IMAGE_TOO_LARGE,
    IMAGE_INVALID,
    IMAGE_SOURCE_URL_INVALID,
    IMAGE_DOWNLOAD_IN_PROGRESS,
    IMAGE_RENDITION_SIZE_INVALID,
    BOARD_DOES_NOT_EXIST,
    BOARD_INSUFFICIENT_PERMISSIONS,
    BOARD_NOT_SOFT_DELETED,
    BOARD_ALREADY_SOFT_DELETED,
    BOARD_NAME_ALREADY_EXISTS,
    REAUTHENTICATION_FAILED,
    PASSWORD_PREVIOUSLY_USED,
    PASSWORD_CHANGED_TOO_SOON,
    PASSWORD_CHANGE_COLLISION,
    UNSUPPORTED_REAUTHENTICATION_FACTOR,
    EXPORT_ALREADY_IN_PROGRESS,
    EXPORT_TOO_SOON,
    EXPORT_DOES_NOT_EXIST,
    EXPORT_INSUFFICIENT_PERMISSIONS,
    EXPORT_NOT_READY,
    EXPORT_GONE,
    IMPORT_ALREADY_IN_PROGRESS,
    IMPORT_DOES_NOT_EXIST,
    IMPORT_INSUFFICIENT_PERMISSIONS,
    IMPORT_NOT_AWAITING_ARCHIVE,
    IMPORT_CHUNK_OFFSET_MISMATCH,
    IMPORT_ARCHIVE_EMPTY,
    IMPORT_ARCHIVE_TOO_LARGE,
    IMPORT_INSUFFICIENT_STORAGE,
    TOO_MANY_AUTHENTICATION_ATTEMPTS,
}
