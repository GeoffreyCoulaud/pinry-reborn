name: Release

on:
  push:
    branches:
      - main
    tags:
      - "v*"
    # Skip the release (image rebuild + publish) when a push touches only docs:
    # nothing shippable changed. A push touching any non-doc file still triggers.
    paths-ignore:
      - "**/*.md"
      - "docs/**"

permissions:
  contents: read

jobs:
  # Same quality gate as on PRs. Job name must stay "validate" so it produces
  # the "validate / gate" check. push=true publishes the multi-arch image to
  # GHCR; packages:write lets the reusable verify job push, id-token:write lets
  # it sign the SBOM/VEX attestations via keyless cosign, and actions:write lets
  # it delete the engine state entries its own save replaces.
  validate:
    permissions:
      contents: read
      packages: write
      id-token: write
      actions: write
    uses: ./.github/workflows/validate.yml
    with:
      push: true
