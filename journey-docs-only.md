# Throwaway journey

This file exists to exercise the documentation-only path of `validate.yml` after
`docs/adr/0031-the-gate-builds-once-and-keeps-its-cache.md` merged the two jobs into one. It is a
root-level markdown file, so `dagger call prose` reads it rather than skipping it the way it skips a
dated document, which makes it a stronger subject than the handoff that pull request #119 carried.

The branch is deleted and the pull request closed once the run is read. Nothing here is ever merged.
